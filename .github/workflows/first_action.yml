name: first_action_workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello! Welcome"
      - name: Print Goodbye
        run: echo "Done - Bye!"
